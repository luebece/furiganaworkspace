# GitHub Pages 배포

1. GitHub에서 새 공개 저장소를 만든다. 예: `furiganaworkspace`.
2. 이 폴더 내용을 저장소에 올린다.
3. `Settings` → `Pages` → `Build and deployment`에서 `Deploy from a branch` 선택.
4. 브랜치 `main`, 폴더 `/site` 선택 후 `Save`.
5. 1~2분 뒤 페이지 주소가 생성된다.

예시 주소:
- `https://<github-id>.github.io/furiganaworkspace/`
